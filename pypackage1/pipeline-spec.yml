version: '1.0'
kind: pipeline
metadata:
  name: ngci-test/pypackage1
  description: "pypackage1"
  deprecate: {}
  project: ngci-test
spec:
  triggers:
    - type: git
      repo: rfblue2/ngci-test
      events:
        - push
      branchRegex: /.*/gi
      modifiedFilesGlob: 'pypackage1/**'
      provider: github
      name: my-trigger
      context: ngci-test
  contexts: []
  steps:
    clone:
      title: Clone
      type: git-clone
      repo: "rfblue2/ngci-test"
      revision: '${{CF_BRANCH}}'
      git: github 
    test:
      title: Test
      type: freestyle
      image: "l.gcr.io/google/bazel:3.1.0"
      working_directory: "${{clone}}"
      command:
        - ls
        - bazel test //pypackage1/...   
  stages: []
